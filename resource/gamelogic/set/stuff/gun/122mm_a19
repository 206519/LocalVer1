;_________________________________________
; ARTY - USSR Heavy AT gun / Howitzer hybrid
;=========================================


{from "pattern gun"			; L/48 "A 19"
	{filling "bulletrus_122_l48"}
	{fireSound "cannon/s4/d1/Shared/Cannon_105to130mm/"}
	{fireSoundclose "cannon/s4/3p/Shared/Cannon_105to130mm/"}
	{reloadSound "utility/"}
	("heavy_artillery")
	{calibre 122}
	;{range 465 465} ; // 20.4km
	{range 635 635} ; // 20.4km

	("reload time" seconds(20))

	("velocity m/s" ap(795) apcr(1) he(795) heat(1))
	("armor health" damage(390))
	("armor health custom" shell(aphe) damage(390) filler(211))
	("armor health custom" shell(aphebc) damage(390) filler(211))
	("122_rus_slope_multipliers")

	; BR-471
	("penetration_long" shell(aphe) range(230) a(223.6) b(208) c(180) d(159.7) e(141))  ; // RHA

	; BR-471B ;issued sometime between Sept. 1944 and Jan. 1945
	("penetration_long" shell(aphebc) range(230) a(205) b(197) c(184) d(171) e(160))  ; // RHA

	("accuracy_indirect"   ; // Applies logic for accuracy of indirect weapons over 200m
		r1(0.65) 				; // Accuracy radius for direct-fire munitions
		r2(6) 					; // Accuracy radius for indirect fire
		rmax(37) 				; // Accuracy radius for indirect fire at maximum range
		range_ap(230) 			; // Range after which accuracy drops to prevent farshooting.
		range_heat(230) 		; // Range after which accuracy drops to prevent farshooting.
		range_he(635)			; // Range after which accuracy drops to prevent farshooting.
	)
}
